# ==========================================
# CẤU HÌNH GỬI EMAIL OTP (DÀNH CHO ĐĂNG KÝ/ĐĂNG NHẬP)
# ==========================================

# 1. Giai đoạn đầu (Ít khách, dùng Gmail miễn phí):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=email_cua_ban@gmail.com
SMTP_PASS=mat_khau_ung_dung_gmail_16_ky_tu_viet_lien_khong_dau

# 2. Giai đoạn sau (Hàng ngàn khách/ngày, dùng dịch vụ chuyên nghiệp SendGrid/Amazon SES):
# Bạn chỉ cần đổi SMTP_HOST thành host của dịch vụ mới (VD: smtp.sendgrid.net)
# Đổi SMTP_USER và SMTP_PASS thành API Key của dịch vụ đó.
# Mọi logic code không cần phải lập trình lại!

# Lưu ý: 
# - Nếu chạy trên máy local (VS Code), hãy copy nội dung file này sang một file mới tên là `.env`
# - KHÔNG BAO GIỜ được gửi file `.env` chứa mật khẩu thật lên GitHub hoặc chia sẻ cho người khác.
